plugins {
    id "com.android.library"
    id "org.jetbrains.kotlin.android"
    id "org.jetbrains.kotlin.kapt"
}

android {
    namespace "com.hejwesele.cms"

    buildTypes {
        qa {
            matchingFallbacks = ["release"]
        }
    }
}

dependencies {
    implementation projects.libraries.encryption
    implementation projects.libraries.intent

    implementation projects.data.repositories.details
    implementation projects.data.repositories.events
    implementation projects.data.repositories.galleries
    implementation projects.data.repositories.invitations
    implementation projects.data.repositories.schedules
    implementation projects.data.repositories.services

    implementation libs.kotlinx.coroutines.core
    implementation libs.kotlinx.datetime

    implementation libs.hilt.core
    kapt libs.hilt.compiler
}
